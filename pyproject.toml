[project]
name = "ai-data-quality-agent"
version = "0.1.0"
description = "Explainable data quality validation and anomaly reporting"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
