# Security Policy

- Never commit credentials, private datasets, or customer records.
- Treat uploaded data as sensitive until classified.
- LLM explanations must not replace deterministic validation.
- Do not expose secrets through logs or generated reports.
